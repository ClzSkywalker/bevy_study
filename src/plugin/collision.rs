use bevy::prelude::*;
// use bevy_rapier2d::prelude::*;

pub struct CollisionPlugin;

impl Plugin for CollisionPlugin {
    fn build(&self, _: &mut App) {
    }
}

// fn collision(collision_events: EventReader<CollisionEvent>){

// }